apiVersion: v2
name: gateway
description: Platform-owned edge resources - GatewayClass, the HTTPS Gateway, global Kong tracing and gateway network policies.
type: application
version: 1.0.0
appVersion: "3.9.3"
